<% if current_user %>
  <%= form_for(@side) do |f| %>
    <% if @side.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@side.errors.count, "error") %> prohibited this side from being saved:</h2>

        <ul>
        <% @side.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= f.hidden_field :condition_id, :value => params[:condition_id]%>
    </div>
    <div class="field">
      <%= f.label :body %><br>
      <%= f.text_area :body %>
    </div>
    <div class="field">
      <%= f.label :summary %><br>
      <%= f.text_field :summary %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<% end %>
