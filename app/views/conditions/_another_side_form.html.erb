<%= form_for(@side, :remote => true) do |f| %>
  <% if @side.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@side.errors.count, "error") %> prohibited this side from being saved:</h2>

      <ul>
      <% @side.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.hidden_field :is_left, :value => local_assigns[:is_left] %>
  </div>
  <div class="field">
    <%= f.hidden_field :condition_id, :value => local_assigns[:condition_id] %>
  </div>
  <div class="field">
    <%= f.label :add_a_reason_in_favor_of_peritoneal_dialysis %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
